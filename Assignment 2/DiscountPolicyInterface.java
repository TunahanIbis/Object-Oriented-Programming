// Problem IV
// Define DiscountPolicy as an interface instead of the abstract class described in problem 1.
public interface DiscountPolicyInterface {
    
    double computeDiscount(int count, double itemCost);
}
