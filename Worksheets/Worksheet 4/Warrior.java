import javax.sound.midi.MidiDeviceTransmitter;

public class Warrior{
    private String name;
    private int health;
    private int stamina;
    private int strength;

public void slash(int range [1,6], int strength, int stamina) {
this.strength = strength;
this.stamina = stamina;
if (stamina > 0);
return;

    int damage(int random [1,6] * int strength)

else if(stamina <= 0)
meditate;

}

public meditate(int random [1,3]);
return (stamina + random [1,3]);
}
}